[package]
name = "goldev"
version = "0.1.0"
edition = "2021"
description = "Goldev — portfolio of Golfredo Pérez Fernández / GoldevLab."

[dependencies]
resuma = { git = "https://github.com/GoldevLab/resuma.git", version = "1.3.1" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
